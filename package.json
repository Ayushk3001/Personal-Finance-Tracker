{
  "name": "personal-finance-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Personal finance tracker with a Prisma and PostgreSQL backend",
  "scripts": {
    "dev": "npx vercel dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "prisma:generate": "prisma generate",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
